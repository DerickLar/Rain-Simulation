#include "ofMain.h"
#include "ofApp.h"

//========================================================================
int main( ){
	ofSetupOpenGL(1000, 1000, OF_WINDOW); // Set up openGL window
	ofRunApp(new ofApp()); // Run app
}
